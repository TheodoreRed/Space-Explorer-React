export default interface AstronomyPictureOfTheDay {
  copyright: string;
  date: string;
  explanation: string;
  hdurl: string;
  media_type: string;
  service_version: string;
  title: string;
  url: string;
}
// useRef() to get the picture from the day before to only show images on the home page
